CREATE TABLE comentario (id BIGSERIAL, autor VARCHAR(255), email VARCHAR(255) NOT NULL, texto VARCHAR(4000) NOT NULL, PRIMARY KEY(id));
CREATE TABLE post (id BIGSERIAL, titulo VARCHAR(255) NOT NULL, resumen VARCHAR(400), texto VARCHAR(4000) NOT NULL, PRIMARY KEY(id));
