CREATE TABLE comentario (post_id INT, id BIGINT, autor VARCHAR(255), email VARCHAR(255) NOT NULL, texto VARCHAR(4000) NOT NULL, PRIMARY KEY(id));
CREATE TABLE post (id INT, titulo VARCHAR(255) NOT NULL, resumen VARCHAR(4000), texto VARCHAR(4000) NOT NULL, PRIMARY KEY(id));
